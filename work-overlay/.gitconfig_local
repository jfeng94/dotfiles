# .gitconfig_local — Work machine git identity
# Included by ~/.gitconfig via [include] path = ~/.gitconfig_local

[user]
	email = jerry.feng@skydio.com
	# Override signingKey here if work machine uses a different SSH key (e.g. YubiKey)
	# signingKey = ~/.ssh/id_work_ed25519.pub

[pull]
	rebase = true

[rebase]
	autoStash = true
